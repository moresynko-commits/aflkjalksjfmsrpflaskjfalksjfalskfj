services:
  - type: "worker" 
    name: msrp-systems
    env: node
    plan: free
    buildCommand: npm ci
    startCommand: node index.js
    envVars:
      - key: DISCORD_TOKEN
        sync: false
    autoDeploy: true

